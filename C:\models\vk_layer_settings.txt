# Vulkan validation layer settings — read by the loader from the process CWD.
# Works under elevated/admin sessions where VK_* env vars are ignored.
# Run R with working directory = the folder containing this file (C:\models).

# --- force the validation layer ON (no env needed) ---
khronos_validation.enable = true

# --- where validation writes its messages ---
# debug_action: log to a file (not just the debugger/stderr)
khronos_validation.debug_action = VK_DBG_LAYER_ACTION_LOG_MSG
khronos_validation.log_filename = C:/models/vk_validation_layer.log

# --- what to report: errors + warnings + perf + best practices + sync ---
khronos_validation.report_flags = error,warn,perf
khronos_validation.enables = VK_VALIDATION_FEATURE_ENABLE_BEST_PRACTICES_EXT,VK_VALIDATION_FEATURE_ENABLE_SYNCHRONIZATION_VALIDATION_EXT

# core validation checks
khronos_validation.validate_core = true
khronos_validation.check_command_buffers = true
khronos_validation.check_object_in_use = true
khronos_validation.check_query = true
khronos_validation.check_shaders = true

# do not break into debugger on error (would hang headless R)
khronos_validation.message_id_filter =
