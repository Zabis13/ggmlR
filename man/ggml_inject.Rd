% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_inject.R
\name{ggml_inject}
\alias{ggml_inject}
\alias{ggml_inject.Seurat}
\title{Inject a single-cell result back into its container}
\usage{
ggml_inject(
  x,
  result,
  reduction_name = "ggml",
  key = "GGML_",
  assay = NULL,
  ...
)

\method{ggml_inject}{Seurat}(
  x,
  result,
  reduction_name = "ggml",
  key = "GGML_",
  assay = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{Seurat} object (the one the data was extracted from).}

\item{result}{A \code{\link{ggml_result}}.}

\item{reduction_name}{Name of the reduction slot to create, e.g.
\code{"ggml"} (default). For Seurat this becomes \code{x[["ggml"]]}.}

\item{key}{Column-name prefix for the embedding, e.g. \code{"GGML_"}.}

\item{assay}{Assay to associate the reduction with (Seurat). Defaults to the
object's default assay.}

\item{...}{Passed to methods.}
}
\value{
The updated container.
}
\description{
Writes the embedding from a \code{\link{ggml_result}} into the standard
dimensionality-reduction slot of a Seurat object (a
\code{SingleCellExperiment} method is added in a later release), returning the
updated object. Component standard deviations and the backend used are
recorded alongside so downstream tools and the user can see how the
reduction was produced.
}
