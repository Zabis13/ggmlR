% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_seurat.R
\name{RunGGML}
\alias{RunGGML}
\alias{RunGGML.default}
\alias{RunGGML.Seurat}
\title{Run a GGML GPU operation on a Seurat object}
\usage{
RunGGML(
  object,
  op = "embed",
  assay = NULL,
  layer = "data",
  n_components = 50L,
  reduction_name = "ggml",
  device = "auto",
  genes = NULL,
  cells = NULL,
  ...
)

\method{RunGGML}{default}(
  object,
  op = "embed",
  assay = NULL,
  layer = "data",
  n_components = 50L,
  reduction_name = "ggml",
  device = "auto",
  genes = NULL,
  cells = NULL,
  ...
)

\method{RunGGML}{Seurat}(
  object,
  op = "embed",
  assay = NULL,
  layer = "data",
  n_components = 50L,
  reduction_name = "ggml",
  device = "auto",
  genes = NULL,
  cells = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{Seurat} object, or a bare feature-by-cell
\code{matrix}/\code{dgCMatrix} (the \code{.default} method returns a
\code{\link{ggml_result}} instead of an object).}

\item{op}{Operation name; see \code{\link{ggml_ops_registry}}. Default
\code{"embed"}.}

\item{assay}{Assay to read (Seurat); defaults to the object's default assay.}

\item{layer}{Layer/slot to read; default \code{"data"}.}

\item{n_components}{Number of components for \code{"embed"} (PCA). Default 50.}

\item{reduction_name}{Name of the reduction slot to create. Default
\code{"ggml"}.}

\item{device}{\code{"auto"} (default), \code{"vulkan"} or \code{"cpu"}.}

\item{genes, cells}{Optional feature/cell subsets passed to extraction.}

\item{...}{Additional parameters forwarded to the engine.}
}
\value{
For a Seurat object, the updated object with a new reduction. For a
  bare matrix, a \code{\link{ggml_result}}.
}
\description{
High-level, Seurat-style entry point: extracts the expression matrix from the
object, runs the requested operation on the GGML backend (Vulkan GPU with CPU
fallback) and writes the result back as a dimensionality reduction. Returns
the updated Seurat object, so it slots into a \code{\%>\%} / \code{|>}
pipeline next to \code{Seurat::RunPCA()}.
}
\details{
The first supported operation is \code{"embed"} (PCA): the gene-by-gene
covariance multiply runs on the GPU, the eigendecomposition on the CPU.
}
\examples{
\dontrun{
library(Seurat)
pbmc <- RunGGML(pbmc, op = "embed", n_components = 30)
DimPlot(pbmc, reduction = "ggml")
}
}
