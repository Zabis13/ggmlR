% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_extract.R
\name{ggml_extract}
\alias{ggml_extract}
\alias{ggml_extract.matrix}
\alias{ggml_extract.dgCMatrix}
\alias{ggml_extract.Seurat}
\title{Extract a feature-by-cell matrix from a single-cell container}
\usage{
ggml_extract(x, assay = NULL, layer = "data", genes = NULL, cells = NULL, ...)

\method{ggml_extract}{matrix}(x, assay = NULL, layer = "data", genes = NULL, cells = NULL, ...)

\method{ggml_extract}{dgCMatrix}(x, assay = NULL, layer = "data", genes = NULL, cells = NULL, ...)

\method{ggml_extract}{Seurat}(x, assay = NULL, layer = "data", genes = NULL, cells = NULL, ...)
}
\arguments{
\item{x}{A \code{Seurat} object, \code{dgCMatrix} or \code{matrix}.}

\item{assay}{Assay to read from. Seurat: defaults to the object's default
assay. Ignored for bare matrices.}

\item{layer}{Layer / slot to read. Seurat v5: a layer name (default
\code{"data"}); Seurat v4: mapped to the \code{slot} argument of
\code{GetAssayData}. Ignored for bare matrices.}

\item{genes}{Optional character/integer vector selecting feature rows.}

\item{cells}{Optional character/integer vector selecting cell columns.}

\item{...}{Passed to methods.}
}
\value{
A dense numeric \code{matrix}, features x cells.
}
\description{
Pulls an expression matrix out of a Seurat object, a
\code{SingleCellExperiment} (added in a later release), a sparse
\code{dgCMatrix} or a plain \code{matrix}, returning a dense numeric matrix
with features in rows and cells in columns — the layout the GPU engine
expects. Optional \code{genes}/\code{cells} subsetting happens before any
sparse-to-dense materialisation.
}
